<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ siteConfig.title }}</title>
    <link rel="stylesheet" href="/css/style.css" />

    <script
      defer
      data-domain="aoc-24.nicole.computer"
      data-api="https://lively-dew-5ace.nicole-bda.workers.dev/api/event"
      src="https://lively-dew-5ace.nicole-bda.workers.dev/js/script.js"
    ></script>
  </head>
  <body class="min-h-screen bg-gray-50">
    <div class="container px-4 py-8 mx-auto">
      <h1 class="mb-8 text-4xl font-bold text-gray-900">
        {{ siteConfig.title }}
      </h1>
      <ul class="space-y-4">
        {% for problem in collections.problems | sort(attribute='data.title') %}
        <li
          class="p-4 transition-shadow bg-white rounded-lg shadow hover:shadow-md"
        >
          <a
            href="{{ problem.data.page.url }}"
            class="text-lg font-medium text-blue-600 hover:text-blue-800"
          >
            {{ problem.data.title }}
          </a>
          {% if problem.data.difficulty %}
          <span
            class="ml-2 px-2 py-1 text-sm rounded-full {% if problem.data.difficulty == 'Easy' %} bg-green-100 text-green-800 {% elif problem.data.difficulty == 'Medium' %} bg-yellow-100 text-yellow-800 {% else %} bg-red-100 text-red-800 {% endif %}"
          >
            {{ problem.data.difficulty }}
          </span>
          {% endif %}
        </li>
        {% endfor %}
      </ul>
    </div>
  </body>
</html>
