<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ siteConfig.title }}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-4xl font-bold mb-8 text-gray-900">{{ siteConfig.title }}</h1>
        <ul class="space-y-4">
        {% for problem in collections.problems | sort(attribute='data.title') %}
            <li class="bg-white shadow rounded-lg p-4 hover:shadow-md transition-shadow">
                <a href="{{ problem.data.page.url }}" class="text-blue-600 hover:text-blue-800 text-lg font-medium">
                    {{ problem.data.title }}
                </a>
                {% if problem.data.difficulty %}
                <span class="ml-2 px-2 py-1 text-sm rounded-full
                    {% if problem.data.difficulty == 'Easy' %}
                        bg-green-100 text-green-800
                    {% elif problem.data.difficulty == 'Medium' %}
                        bg-yellow-100 text-yellow-800
                    {% else %}
                        bg-red-100 text-red-800
                    {% endif %}
                ">
                    {{ problem.data.difficulty }}
                </span>
                {% endif %}
            </li>
        {% endfor %}
        </ul>
    </div>
</body>
</html>
